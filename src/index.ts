let message: string = '%cTypescript is running';

console.log(message, 'color: green; font-size: 30px')

// let heading = document.createElement('h1');
// heading.textContent = message;

// document.body.appendChild(heading);